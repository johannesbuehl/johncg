builds:
  server:
    script: server-build
    entry: main.js
    script_path: JohnCG_0.9.0-rc.1_win32
    files:
      - main.js
  client:
    script: client-build
    files:
      - client
  templates:
    script: templates-build
    files: []
  pandoc:
    script: pandoc-build
    entry: pandoc/pandoc-installer.js
    script_path: pandoc/pandoc-installer
    files:
      - pandoc
configs:
  server:
    schema: config.schema.json
    orig: config_default.yaml
    dest: config.yaml
external_packages:
  - "@img"
  - canvas
  - pdfjs-dist
copy:
  - orig: casparcg
  - orig: files/Bible
    dest: Bibles
  - orig: files/Psalm
    dest: Psalms
  - orig: files/Song
    dest: Songs
  - orig: pandoc/texlive.profile
mkdir:
  - Playlists
  - PDFs
release_dir: dist
license_report:
  config:
    ignore:
      - dist/*
  path: licenses
  license:
    path: LICENSE
